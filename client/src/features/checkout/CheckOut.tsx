import { Typography } from "@mui/material";

export default function checkOut() {

    return(

        <Typography variant='h4'>
        Only registered users should be at this page
        </Typography>
    )
    
}